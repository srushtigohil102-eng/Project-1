{
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/supertest": "^7.2.0",
    "jest": "^30.4.2",
    "mongodb-memory-server": "^11.2.0",
    "supertest": "^7.2.2",
    "ts-jest": "^29.4.11"
  },
"scripts": {
  "dev": "nodemon src/server.ts",
  "build": "tsc",
  "lint": "eslint .",
  "test": "jest --runInBand",
  "test": "jest --watch"
}
}
